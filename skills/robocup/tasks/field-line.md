# Field line

Submit a coarse polyline along each painted white field line. The Rust pixel refiner will search normal to the coarse path, limit evidence to the field polygon, smooth outliers, and report white-pixel support.

