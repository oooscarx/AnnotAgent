# Field region

Outline the visible playable green surface as a polygon. Occluded boundaries may be inferred conservatively, but the polygon must remain inside the image. This result constrains later object and line checks.

