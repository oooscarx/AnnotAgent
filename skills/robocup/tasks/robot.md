# Robot attributes

For every robot, provide `team_color` (`red`, `blue`, or `unknown`) and `state` (`standing`, `fallen`, `goalkeeper`, `unavailable`, or `unknown`). Torso color evidence is deterministic assistance; state remains a VLM judgment with geometry warnings.

