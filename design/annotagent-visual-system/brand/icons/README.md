# Iconography

- 24×24 viewBox。
- 1.75px 线宽。
- round cap / round join。
- 使用 `currentColor`，由上下文控制颜色。
- 图标必须搭配 accessible label 或 `aria-label`。
- 不要单靠图标表达高风险操作；Cancel、Delete、Reject 等必须有文字或 tooltip。
- `annotagent-icons.svg` 提供 symbol sprite；单独 SVG 适合 `<img>` 或构建期导入。
