# RoboCup AnnotAgent

A VLM-powered annotation and quality-control agent for RoboCup robot perception datasets, built on the extensible AnnotAgent Core.

This repository is being implemented as a Rust-first course project. The checked-in implementation plan and requirement matrix define the staged, test-first delivery path; runnable instructions will be expanded as each vertical slice lands.

