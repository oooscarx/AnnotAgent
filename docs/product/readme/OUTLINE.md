# P0 文案与结构

首屏标题：AnnotAgent
定位：从原始图片到训练数据包的视觉数据 Agent。
正文：告诉 AnnotAgent 用哪些图片、标注什么、准备训练什么。LLM 组织视觉模型和处理工具，你在需要时检查与修正，再导出目标格式的数据。
范围：Alpha；需配置模型，保留样例验证和人工审核。

顺序：首屏 → 同一任务四步 → 三个产品价值 → 唯一源码启动方式 → 模型与导出范围 → 职责图 → 限制、文档、贡献与许可。

截图原则：现有 B-Human 案例可证明样例处理，未核实完整正式包前不描述为端到端自动成功。最终集成页面尚待 Frontend 1 指定；本地 8788 仍为旧服务，不能冠以最终版本。
