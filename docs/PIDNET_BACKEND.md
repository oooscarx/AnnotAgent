# PIDNet Backend

The native package is `org.annotagent.pidnet-onnx`. It runs the declared ONNX input/logit contract
through the Rust runtime and returns a lineage-preserving SemanticMask. Real external checkpoint
smoke remains live-conditional.

See [PIDNet Rust Plugin](PIDNET_RUST_PLUGIN.md).
