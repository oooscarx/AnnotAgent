# RF-DETR Backend

The native package is `org.annotagent.rfdetr-onnx`. Its Rust implementation follows the official
fixed NCHW detection export and typed DetectionSet contract. A real export/sample smoke is
live-conditional and the package cannot become Ready without exact checkpoint evidence.

See [RF-DETR Rust Plugin](RFDETR_RUST_PLUGIN.md).
