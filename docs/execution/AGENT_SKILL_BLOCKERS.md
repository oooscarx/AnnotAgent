# AnnotAgent Agent + Skill Blockers

No implementation blocker is active.

Live external provider acceptance is conditional on operator-supplied credentials and services.
It is not required for offline release acceptance and no key from conversation history is allowed.
